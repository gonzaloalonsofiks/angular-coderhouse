// Employees
export interface Employee {
  firstName: string;
  lastName: string;
  email: string;
  gender: string;
  jobTitle: string;
  jobArea: string;
  contractType: string;
  active: boolean;
}

// Navigation Menu Links
export interface NavMenuLink {
  name: string;
  url: string;
}
